services:
  portfolio:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - backend/.env
    volumes:
      # Les JSON sont montés en volume :
      # tu peux les éditer sans rebuild l'image
      - ./src/data:/app/src/data
    restart: unless-stopped
